import React from 'react';

const ContactPage = () => (
    <div>
        <h1>Contact</h1>
    </div>
);

ContactPage.settings = {
    title: 'Contact',
    backgroundTitle: 'Contact',
};

export default ContactPage;
